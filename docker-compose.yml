services:
  starship-sheet:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
      - DB_PATH=/app/data/starship-sheets.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
